# Local measurement and evaluation stack. Python >= 3.10.
# Training is not done locally: see notebooks/01_training_colab.ipynb.
numpy>=2.0
onnxruntime>=1.26
onnx>=1.16
opencv-python-headless>=4.10
pillow>=10.0
pyyaml>=6.0
sympy>=1.13          # required by onnxruntime's quantization preprocessing
matplotlib>=3.8      # figures only
roboflow>=1.1        # dataset download only
supervision>=0.30    # Detections container used by the tracker
trackers>=2.6        # ByteTrackTracker, tracking only
pytest>=8.0          # tests only
